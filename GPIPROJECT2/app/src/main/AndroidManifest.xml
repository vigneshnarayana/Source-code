<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gpiproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GPIPROJECT"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.Weightment"></activity>
        <activity android:name=".view.Purchase" />
        <activity android:name=".view.LotCreation" />
        <activity android:name=".view.FarmerPurchase"/>
        <activity android:name=".view.HeaderCreation"/>
        <activity android:name=".view.LandingPage"/>
        <activity android:name=".view.UserLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>